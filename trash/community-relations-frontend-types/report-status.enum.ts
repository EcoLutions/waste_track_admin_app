/**
 * Report status enumeration
 * Corresponds to: ReportStatus.java enum
 *
 * @see ReportStatus.java in backend
 */
export enum ReportStatusEnum {
  SUBMITTED = 'submitted',
  ACKNOWLEDGED = 'acknowledged',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'resolved',
  REJECTED = 'rejected'
}