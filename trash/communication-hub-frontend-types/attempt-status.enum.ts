/**
 * Attempt status enumeration
 * Corresponds to: AttemptStatus.java enum
 *
 * @see AttemptStatus.java in backend
 */
export enum AttemptStatusEnum {
  PENDING = 'pending',
  DELIVERED = 'delivered',
  FAILED = 'failed',
  BOUNCED = 'bounced'
}