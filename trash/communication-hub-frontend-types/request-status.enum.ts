/**
 * Request status enumeration
 * Corresponds to: RequestStatus.java enum
 *
 * @see RequestStatus.java in backend
 */
export enum RequestStatusEnum {
  PENDING = 'pending',
  SENT = 'sent',
  FAILED = 'failed',
  EXPIRED = 'expired'
}